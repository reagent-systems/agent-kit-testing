// The site is content from the repo. Everything renders at build time.
export const prerender = true;

// A directory per page (`docs/index.html`, not `docs.html`). Every static
// host serves that shape without extensionless-URL rules of its own.
export const trailingSlash = 'always';
